---
title: "Ejemplo: Clasificación con scikit-learn"
description: "Tutorial básico de clasificación usando scikit-learn"
author: "Alex de la Puente"
date: "2026-01-25"
categories: [machine-learning, python, tutorial]
image: ""
---

## Introducción

Este es un ejemplo de post sobre Machine Learning. Aquí puedes combinar texto explicativo con código ejecutable.

## Código de ejemplo

```{python}
#| echo: true
#| eval: false

from sklearn.datasets import load_iris
from sklearn.model_selection import train_test_split
from sklearn.ensemble import RandomForestClassifier
from sklearn.metrics import accuracy_score

# Cargar datos
iris = load_iris()
X_train, X_test, y_train, y_test = train_test_split(
    iris.data, iris.target, test_size=0.2, random_state=42
)

# Entrenar modelo
clf = RandomForestClassifier(n_estimators=100, random_state=42)
clf.fit(X_train, y_train)

# Evaluar
y_pred = clf.predict(X_test)
accuracy = accuracy_score(y_test, y_pred)
print(f"Accuracy: {accuracy:.2f}")
```

## Conclusiones

Este es solo un ejemplo de cómo estructurar tus notas. Puedes incluir:

- Código ejecutable (con `eval: true` si quieres que se ejecute)
- Matemáticas con LaTeX: $f(x) = \frac{1}{1 + e^{-x}}$
- Imágenes, tablas, gráficos, etc.

## Referencias

- [scikit-learn documentation](https://scikit-learn.org/)
